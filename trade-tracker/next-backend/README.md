Next backend for Trade Tracker

Run locally:

```powershell
cd next-backend; npm install; npm run dev
```

This starts Next on port 3001 (so it doesn't conflict with the frontend). It provides a demo API at `/api/summary` which returns a sample tiles array and computed average.
